"""Vault module tests.

T-06 acceptance: every entry point in :mod:`src.vault.builder` and
:mod:`src.vault.validator` has unit coverage, plus a manifest-only
round-trip in :mod:`test_manifest` that catches schema drift.
"""
