{
  "name": "skillsgit",
  "private": true,
  "description": "Skills Marketplace monorepo — two-sided marketplace for AI skills (skills.md).",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "concurrently -n api,marketplace,creator -c blue,green,magenta \"pnpm --filter @skillsgit/api dev\" \"pnpm --filter @skillsgit/web-marketplace dev\" \"pnpm --filter @skillsgit/web-creator dev\"",
    "build": "pnpm -r --parallel build",
    "lint": "pnpm -r --parallel lint",
    "typecheck": "pnpm -r --parallel typecheck",
    "test": "pnpm -r --parallel test",
    "format": "prettier --write \".\"",
    "format:check": "prettier --check \".\"",
    "codegen": "echo \"# TODO: regenerate api-client from /v1/openapi.json\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "husky": "^9.1.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0"
  }
}
