"""Auth — register/login/logout, sessions, password reset, email verification,
API tokens, become-creator. OAuth scaffolded (Google/GitHub) — returns 501
when client credentials are not configured.

Spec: ``shared/auth.md``.
"""
